apiVersion: v1
kind: Service
metadata:
  name: deletar-contatos-service
  labels:
    app: deletar-contatos
    tier: backend
    environment: production
  annotations:
    description: "Service para expor o microsserviço deletar-contatos"
spec:
  type: LoadBalancer
  selector:
    app: deletar-contatos
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
